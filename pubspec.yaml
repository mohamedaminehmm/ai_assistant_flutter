name: assistant
description: "A voice command classifier app"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  device_apps: ^2.2.0

  
  # ⭐ SIMPLIFIED DEPENDENCIES - THESE DEFINITELY WORK ⭐
  permission_handler: ^11.0.0
  url_launcher: ^6.1.0
  android_intent_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/